dist: 'dist'
entrypoint: 'index.js'
tasks:
  test: 'taskkit-shell'
  scripts: 'taskkit-rollup'
  eslint: 'taskkit-eslint'
  watcher: 'taskkit-watcher'
  livereload: 'taskkit-livereload'
  default:
    - 'build'
    - 'test'
  build:
    -
      - 'eslint'
      - 'scripts'
  dev:
    - 'livereload'
    - 'watcher'
scripts:
  name: '{{libraryName}}'
test:
  command: 'npm'
  arguments: 'test'
eslint:
  ignore:
    - '{{dist}}'
    - 'node_modules'
    - '.git'
    - '*.dist.js'
  files:
    - './**/*.js'

watcher:
  ignore:
    - 'node_modules'
    - '.git'
  files:
    './**/*.js': 
      task: 'default'
      ignore: 
        - '{{dist}}'
    '{{dist}}/*': 'livereload'
